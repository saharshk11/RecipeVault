# `recipe_core_wasm`

This folder is reserved for the Rust → WASM build output that exposes recipe extraction to the Cloudflare runtime.

Until the WASM build pipeline is wired up, `index.ts` is a placeholder and the server falls back to a lightweight JSON-LD parser.

